const list=document.querySelector('[data-notification-list]'),statusEl=document.querySelector('[data-notification-status]');let session,markAllBusy=false;const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const setStatus=(message,error=false)=>{if(!statusEl)return;statusEl.textContent=message||'';statusEl.className=error?'small-muted error':'small-muted'};const validDate=value=>{const date=new Date(value);return Number.isNaN(date.getTime())?'Unknown time':date.toLocaleString()};async function getSession(){return window.hotFlashGetStableSession?window.hotFlashGetStableSession():(await hotflashSupabase.auth.getSession()).data.session}async function load(){try{session=await getSession();if(!session){location.href=`login.html?returnTo=${encodeURIComponent(location.pathname+location.search)}`;return}setStatus('Loading notifications…');list.innerHTML='<p class="small-muted">Loading notifications…</p>';const{data,error}=await hotflashSupabase.from('notifications').select('id,type,message,vehicle_id,post_id,comment_id,is_read,created_at,actor:profiles!notifications_actor_id_fkey(username,display_name,avatar_url)').eq('recipient_id',session.user.id).order('created_at',{ascending:false}).limit(100);if(error)throw error;const rows=data||[];setStatus(rows.length?'':'No notifications yet.');list.innerHTML=rows.map(n=>`<article class="notification-card ${n.is_read?'':'unread'}" data-id="${esc(n.id)}"><div class="social-post-head"><div class="social-avatar">${esc((n.actor?.display_name||n.actor?.username||'H')[0])}</div><div><strong>${esc(n.actor?.display_name||n.actor?.username||'Hot Flash member')}</strong> ${esc(n.message||n.type||'sent an update')}<div class="small-muted">${esc(validDate(n.created_at))}</div></div></div><div class="social-actions">${n.vehicle_id?`<a class="secondary-button" href="vehicle.html?id=${encodeURIComponent(n.vehicle_id)}">Open build</a>`:''}${n.actor?.username?`<a class="secondary-button" href="member.html?u=${encodeURIComponent(n.actor.username)}">View member</a>`:''}${n.is_read?'':`<button type="button" data-read>Mark read</button>`}</div></article>`).join('')||'<p class="small-muted">No notifications yet.</p>';list.querySelectorAll('[data-read]').forEach(button=>button.addEventListener('click',async()=>{if(button.disabled)return;const card=button.closest('[data-id]');button.disabled=true;button.textContent='Saving…';const{error}=await hotflashSupabase.from('notifications').update({is_read:true}).eq('id',card.dataset.id).eq('recipient_id',session.user.id);if(error){button.disabled=false;button.textContent='Mark read';setStatus('That notification could not be updated.',true);return}card.classList.remove('unread');button.remove();setStatus('Notification marked as read.')}))}catch(error){console.error('[Hot Flash notifications]',error);setStatus(navigator.onLine?'Notifications could not be loaded. Please refresh and try again.':'You appear to be offline.',true);list.innerHTML='<p class="error">Notifications are unavailable right now.</p>'}}document.querySelector('[data-mark-all-read]')?.addEventListener('click',async event=>{if(!session||markAllBusy)return;markAllBusy=true;const button=event.currentTarget,original=button.textContent;button.disabled=true;button.textContent='Marking…';const{error}=await hotflashSupabase.from('notifications').update({is_read:true}).eq('recipient_id',session.user.id).eq('is_read',false);if(error){setStatus('Notifications could not be marked as read.',true);button.disabled=false;button.textContent=original;markAllBusy=false;return}await load();button.disabled=false;button.textContent=original;markAllBusy=false});document.addEventListener('DOMContentLoaded',load);