const feed=document.querySelector('[data-hoon-feed]'),composer=document.querySelector('[data-hoon-composer]'),form=document.querySelector('[data-hoon-form]'),statusEl=document.querySelector('[data-hoon-status]'),vehicleSelect=document.querySelector('[data-hoon-vehicle]'),moreButton=document.querySelector('[data-hoon-more]');let session=null,posting=false,offset=0;const PAGE=12;const esc=v=>String(v||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const safeUrl=v=>{try{const u=new URL(String(v||''),location.origin);return ['http:','https:'].includes(u.protocol)?u.href:''}catch{return''}};const labels={track:'Track',drift_event:'Drift event',drag_strip:'Drag strip',private_property:'Private property',off_road_park:'Off-road park',closed_course:'Closed course'};async function getSession(){return window.hotFlashGetStableSession?window.hotFlashGetStableSession():(await hotflashSupabase.auth.getSession()).data.session}function setStatus(message,type=''){statusEl.textContent=message;statusEl.className=`small-muted${type?` ${type}`:''}`}function dateText(value){const d=new Date(value);return Number.isNaN(d.valueOf())?'':d.toLocaleString([],{dateStyle:'medium',timeStyle:'short'})}async function loadVehicles(){session=await getSession();if(!session)return;const{data}=await hotflashSupabase.from('vehicles').select('id,hotflash_id,nickname,year,make,model').eq('owner_id',session.user.id).order('created_at',{ascending:false});vehicleSelect.innerHTML='<option value="">Not linked to a vehicle</option>'+((data||[]).map(v=>`<option value="${esc(v.id)}">${esc(v.nickname||[v.year,v.make,v.model].filter(Boolean).join(' ')||v.hotflash_id)}</option>`).join(''))}async function openComposer(){session=await getSession();if(!session){location.href=`login.html?returnTo=${encodeURIComponent('hoon.html#create')}`;return}composer.hidden=false;await loadVehicles();composer.scrollIntoView({behavior:'smooth',block:'start'})}document.querySelector('[data-open-hoon-composer]')?.addEventListener('click',openComposer);document.querySelector('[data-close-hoon-composer]')?.addEventListener('click',()=>{composer.hidden=true;setStatus('')});async function flameInfo(postIds){if(!postIds.length)return new Map;const{data}=await hotflashSupabase.from('hoon_flames').select('post_id,user_id').in('post_id',postIds);const map=new Map;for(const row of data||[]){const item=map.get(row.post_id)||{count:0,mine:false};item.count++;if(session?.user?.id===row.user_id)item.mine=true;map.set(row.post_id,item)}return map}async function authorInfo(authorIds){const ids=[...new Set(authorIds.filter(Boolean))];if(!ids.length)return new Map;const{data,error}=await hotflashSupabase.from('profiles').select('id,username,display_name').in('id',ids);if(error){console.warn('[Hoon Pad authors]',error);return new Map}return new Map((data||[]).map(profile=>[profile.id,profile]))}async function loadFeed(reset=false){if(reset){offset=0;feed.innerHTML=''}session=await getSession();if(!offset)feed.innerHTML='<div class="hoon-empty">Loading Hoon Pad…</div>';const{data,error}=await hotflashSupabase.from('hoon_posts').select('id,author_id,vehicle_id,video_url,storage_path,caption,venue_type,venue_name,created_at,vehicle:vehicles(id,hotflash_id,nickname,year,make,model)').order('created_at',{ascending:false}).range(offset,offset+PAGE-1);if(error){console.error('[Hoon Pad feed]',error);const missing=error.code==='42P01'||/relation .*hoon_posts.*does not exist/i.test(error.message||'');feed.innerHTML=`<div class="hoon-empty"><strong>Hoon Pad could not load.</strong><p>${missing?'Its database setup is incomplete.':'Please refresh and try again. If it continues, send a feedback report so we can see the browser details.'}</p></div>`;moreButton.hidden=true;return}const posts=data||[];if(reset||offset===0)feed.innerHTML='';if(!posts.length&&offset===0){feed.innerHTML='<div class="hoon-empty"><strong>No clips yet.</strong><p>Be the first to post something from a safe, legal venue.</p></div>';moreButton.hidden=true;return}const[flames,authors]=await Promise.all([flameInfo(posts.map(p=>p.id)),authorInfo(posts.map(p=>p.author_id))]);posts.forEach((post,index)=>{if((offset+index)>0&&(offset+index)%6===0){const divider=document.createElement('aside');divider.className='hoon-divider';divider.innerHTML='<strong>Respect the streets.</strong><span>Save the fun for tracks, private property, and legal events.</span>';feed.appendChild(divider)}const info=flames.get(post.id)||{count:0,mine:false};const author=authors.get(post.author_id);const vehicleName=post.vehicle?.nickname||[post.vehicle?.year,post.vehicle?.make,post.vehicle?.model].filter(Boolean).join(' ');const card=document.createElement('article');card.className='hoon-card';card.dataset.postId=post.id;card.dataset.storagePath=post.storage_path;card.innerHTML=`<video class="hoon-video" controls playsinline preload="metadata" src="${esc(safeUrl(post.video_url))}"></video><div class="hoon-copy"><div class="hoon-meta"><span class="hoon-chip">🟢 ${esc(labels[post.venue_type]||'Legal venue')}</span>${post.venue_name?`<span class="hoon-chip">📍 ${esc(post.venue_name)}</span>`:''}</div><h2>${esc(post.caption||'Fresh from the Hoon Pad')}</h2><p class="small-muted">${esc(author?.display_name||author?.username||'Hot Flash member')} · ${esc(dateText(post.created_at))}</p><div class="hoon-actions"><button type="button" data-flame>${info.mine?'🔥 Flamed':'🔥 Flame'} (${info.count})</button>${post.vehicle?`<a class="secondary-button" href="vehicle.html?hf=${encodeURIComponent(post.vehicle.hotflash_id||post.vehicle.id)}">View vehicle${vehicleName?`: ${esc(vehicleName)}`:''}</a>`:''}${session?.user?.id===post.author_id?'<button type="button" class="danger" data-delete>Delete</button>':''}</div></div>`;feed.appendChild(card);card.querySelector('[data-flame]')?.addEventListener('click',()=>toggleFlame(post.id));card.querySelector('[data-delete]')?.addEventListener('click',()=>deletePost(card,post.id))});offset+=posts.length;moreButton.hidden=posts.length<PAGE}async function toggleFlame(postId){session=await getSession();if(!session){location.href=`login.html?returnTo=${encodeURIComponent(location.pathname)}`;return}const{data}=await hotflashSupabase.from('hoon_flames').select('post_id').eq('post_id',postId).eq('user_id',session.user.id).maybeSingle();const result=data?await hotflashSupabase.from('hoon_flames').delete().eq('post_id',postId).eq('user_id',session.user.id):await hotflashSupabase.from('hoon_flames').insert({post_id:postId,user_id:session.user.id});if(result.error){console.error(result.error);return}await loadFeed(true)}async function deletePost(card,id){if(!confirm('Delete this Hoon Pad clip?'))return;const button=card.querySelector('[data-delete]');button.disabled=true;session=await getSession();const{data:deleted,error}=await hotflashSupabase.from('hoon_posts').delete().eq('id',id).eq('author_id',session.user.id).select('id').maybeSingle();if(error||!deleted){button.disabled=false;return}await hotflashSupabase.storage.from('hoon-videos').remove([card.dataset.storagePath]);card.remove()}form?.addEventListener('submit',async event=>{event.preventDefault();if(posting)return;session=await getSession();if(!session){location.href=`login.html?returnTo=${encodeURIComponent('hoon.html#create')}`;return}const fd=new FormData(form),file=fd.get('video'),button=form.querySelector('button[type="submit"]');if(!file?.size){setStatus('Choose a video first.','error');return}if(!['video/mp4','video/quicktime','video/webm'].includes(file.type)){setStatus('Choose an MP4, MOV, or WebM video.','error');return}if(file.size>100*1024*1024){setStatus('Videos must be 100 MB or smaller.','error');return}if(fd.get('legal_attestation')!=='on'){setStatus('Confirm the clip was recorded in a safe and legal environment.','error');return}posting=true;button.disabled=true;button.textContent='Uploading…';const ext=(file.name.split('.').pop()||'mp4').toLowerCase().replace(/[^a-z0-9]/g,'');const path=`${session.user.id}/${crypto.randomUUID?.()||Date.now()}.${ext}`;try{setStatus('Uploading clip… Keep this page open.');const{error:uploadError}=await hotflashSupabase.storage.from('hoon-videos').upload(path,file,{cacheControl:'3600',upsert:false,contentType:file.type});if(uploadError)throw uploadError;const url=hotflashSupabase.storage.from('hoon-videos').getPublicUrl(path).data.publicUrl;const{error:rowError}=await hotflashSupabase.from('hoon_posts').insert({author_id:session.user.id,vehicle_id:String(fd.get('vehicle_id')||'')||null,video_url:url,storage_path:path,caption:String(fd.get('caption')||'').trim()||null,venue_type:String(fd.get('venue_type')),venue_name:String(fd.get('venue_name')||'').trim()||null,legal_attestation:true,save_to_build_history:fd.get('save_to_build_history')==='on'});if(rowError){await hotflashSupabase.storage.from('hoon-videos').remove([path]);throw rowError}form.reset();setStatus('Posted to Hoon Pad.','success');composer.hidden=true;await loadFeed(true)}catch(error){console.error(error);setStatus(error.message||'The clip could not be posted.','error')}finally{posting=false;button.disabled=false;button.textContent='Post to Hoon Pad'}});moreButton?.addEventListener('click',()=>loadFeed(false));document.addEventListener('DOMContentLoaded',async()=>{await loadFeed(true);if(location.hash==='#create')openComposer()});