const qs=(s)=>document.querySelector(s);const esc=(s)=>String(s||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));let session=null;async function initHome(){session=window.hotFlashGetStableSession?await window.hotFlashGetStableSession():(await hotflashSupabase.auth.getSession()).data.session;const member=qs('[data-member-actions]'),guest=qs('[data-guest-actions]');if(session){member.hidden=false;guest.hidden=true;qs('[data-home-greeting]').textContent='Welcome back to the garage.'}await Promise.all([loadBuilds(),loadEvents(),loadMembers()])}async function loadBuilds(){const root=qs('[data-home-builds]');const{data,error}=await hotflashSupabase.from('vehicles').select('id,hotflash_id,nickname,year,make,model,cover_photo,owner_profile:profiles!vehicles_owner_id_fkey(username)').order('created_at',{ascending:false}).limit(6);if(error||!data?.length){root.innerHTML='<div class="home-empty">The garage is warming up. Open Discover to see what is rolling in.</div>';return}root.innerHTML=data.map(v=>`<article class="home-build-card"><a class="home-build-media" href="vehicle.html?hf=${encodeURIComponent(v.hotflash_id||v.id)}" ${v.cover_photo?`style="background-image:linear-gradient(0deg,rgba(7,8,10,.65),rgba(7,8,10,.04)),url('${v.cover_photo}')"`:''}></a><div class="home-build-body"><p class="home-build-owner">@${esc(v.owner_profile?.username||'member')}</p><h3>${esc(v.nickname||'Untitled build')}</h3><p class="small-muted">${esc([v.year,v.make,v.model].filter(Boolean).join(' ')||'Build story in progress')}</p><a class="text-link" href="vehicle.html?hf=${encodeURIComponent(v.hotflash_id||v.id)}">Open build</a></div></article>`).join('')}async function loadEvents(){const root=qs('[data-home-events]');const{data}=await hotflashSupabase.from('events').select('id,title,starts_at,location,venue_name,event_type').gte('starts_at',new Date().toISOString()).order('starts_at').limit(4);root.innerHTML=data?.length?data.map(e=>`<article class="home-event-card"><time>${new Date(e.starts_at).toLocaleString([], {dateStyle:'medium',timeStyle:'short'})}</time><strong>${esc(e.title)}</strong><p class="small-muted">📍 ${esc(e.venue_name||e.location||'Location coming soon')}</p><a class="text-link" href="events.html">View event</a></article>`).join(''):'<div class="home-empty">No upcoming events loaded yet. Community events will appear here.</div>'}async function loadMembers(){const root=qs('[data-home-members]');const{data}=await hotflashSupabase.from('profiles').select('username,display_name,location,location_visibility').not('username','is',null).order('created_at',{ascending:false}).limit(5);root.innerHTML=data?.length?data.map(p=>`<a class="home-person-card" href="member.html?u=${encodeURIComponent(p.username)}"><strong>${esc(p.display_name||p.username)}</strong><p class="small-muted">@${esc(p.username)}${p.location&&p.location_visibility!=='private'?` · ${esc(p.location)}`:''}</p></a>`).join(''):'<div class="home-empty">New members will show up here.</div>'}document.addEventListener('DOMContentLoaded',initHome);