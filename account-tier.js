(()=>{const root=document.querySelector('[data-tier-sheet]');if(!root||!window.HOTFLASH_ACCOUNT_TIERS)return;const params=new URLSearchParams(location.search),key=params.get('tier')||'free',t=HOTFLASH_ACCOUNT_TIERS[key]||HOTFLASH_ACCOUNT_TIERS.free;const esc=v=>String(v||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));document.title=`${t.name} Features | Hot Flash`;root.innerHTML=`<section class="tier-sheet-header"><div class="tier-sheet-title"><div><span class="tier-badge">${esc(t.badge)}</span><h1>${esc(t.name)}</h1><p class="tier-summary">${esc(t.intro)}</p></div><div class="tier-price">${esc(t.price)}<small>${esc(t.billing)}</small></div></div><p><strong>Best for:</strong> ${esc(t.audience)}</p>${t.requirements?`<div class="tier-requirements"><strong>Eligibility and requirements</strong><ul>${t.requirements.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`:''}<div class="tier-sheet-actions"><button type="button" onclick="window.print()">Print / Save as PDF</button><a class="secondary-button" href="account-types.html">View all account types</a></div></section>${(t.sections||[]).map(s=>`<section class="tier-sheet-section${s.negative?' negative':''}"><h2>${esc(s.title)}</h2><ul>${s.items.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></section>`).join('')}<p class="small-muted">Feature availability may depend on account standing, verification, location, device support, released functionality, and compliance with Hot Flash policies. Paid features and pricing may change with notice.</p>`;if(params.get('print')==='1')setTimeout(()=>window.print(),350)})();