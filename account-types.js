(()=>{if(!document.querySelector('script[data-shared-site-nav]')){const s=document.createElement('script');s.src='site-nav.js?v=3';s.dataset.sharedSiteNav='';document.body.appendChild(s)}const root=document.querySelector('[data-tier-grid]');if(!root||!window.HOTFLASH_ACCOUNT_TIERS)return;const esc=v=>String(v||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));root.innerHTML=Object.entries(HOTFLASH_ACCOUNT_TIERS).map(([key,t])=>{const preview=(t.sections||[]).flatMap(s=>s.negative?[]:s.items).slice(0,5);return `<article class="tier-card" data-accent="${esc(t.accent)}"><div class="tier-card-head"><div><span class="tier-badge">${esc(t.badge)}</span><h2>${esc(t.name)}</h2></div><div class="tier-price">${esc(t.price)}<small>${esc(t.billing)}</small></div></div><p class="tier-summary">${esc(t.audience)}</p><ul class="tier-preview-list">${preview.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><div class="tier-actions"><a class="primary-button" href="account-tier.html?tier=${encodeURIComponent(key)}">View full features</a><a class="secondary-button" href="account-tier.html?tier=${encodeURIComponent(key)}&print=1">Printable / PDF view</a></div></article>`}).join('')})();