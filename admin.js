const adminStatus=document.querySelector('[data-admin-status]');const consoleEl=document.querySelector('[data-admin-console]');const form=document.querySelector('[data-admin-search-form]');const result=document.querySelector('[data-admin-result]');const previewWrap=document.querySelector('[data-admin-preview-wrap]');const renderStatus=document.querySelector('[data-admin-render-status]');let currentVehicle=null,currentProfile=null,currentCanvas=null,busy=false;function setStatus(message,type=''){adminStatus.textContent=message;adminStatus.className=`small-muted${type?` ${type}`:''}`}function setRenderStatus(message,type=''){renderStatus.textContent=message;renderStatus.className=`small-muted${type?` ${type}`:''}`}async function stableSession(){return window.hotFlashGetStableSession?window.hotFlashGetStableSession():(await hotflashSupabase.auth.getSession()).data.session}async function checkAccess(){const session=await stableSession();if(!session){location.replace(`login.html?returnTo=${encodeURIComponent(location.pathname+location.search)}`);return}const{data,error}=await hotflashSupabase.rpc('is_hotflash_admin');if(error)throw error;if(!data){setStatus('This account does not have FlashTag fulfillment access.','error');return}setStatus('Access granted. Search by the permanent Hot Flash ID shown on the order.','success');consoleEl.hidden=false}function vehicleName(vehicle){return vehicle.nickname||[vehicle.year,vehicle.make,vehicle.model].filter(Boolean).join(' ')||'Hot Flash vehicle'}async function renderCanvas(){if(!currentVehicle)throw new Error('Find a vehicle first.');setRenderStatus('Rendering the existing FlashTag…');const canvas=await buildCompleteSticker(currentVehicle,currentProfile||{});currentCanvas=canvas;previewWrap.innerHTML='';const copy=document.createElement('canvas');const max=720,scale=Math.min(1,max/canvas.width);copy.width=Math.round(canvas.width*scale);copy.height=Math.round(canvas.height*scale);copy.getContext('2d').drawImage(canvas,0,0,copy.width,copy.height);copy.setAttribute('aria-label',`FlashTag preview for ${vehicleName(currentVehicle)}`);previewWrap.appendChild(copy);setRenderStatus('Ready to print. This uses the vehicle’s existing permanent ID.','success');return canvas}form?.addEventListener('submit',async event=>{event.preventDefault();if(busy)return;busy=true;const button=form.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Searching…';setStatus('Looking up vehicle…');result.hidden=true;currentCanvas=null;try{const reference=String(new FormData(form).get('reference')||'').trim();const{data,error}=await hotflashSupabase.rpc('admin_get_flashtag_vehicle',{p_reference:reference});if(error)throw error;currentVehicle=data.vehicle;currentProfile=data.profile||{};document.querySelector('[data-admin-vehicle-name]').textContent=vehicleName(currentVehicle);document.querySelector('[data-admin-hotflash-id]').textContent=currentVehicle.hotflash_id||currentVehicle.id;document.querySelector('[data-admin-owner]').textContent=`Current owner: ${currentProfile.display_name||currentProfile.username||'Hot Flash member'}`;document.querySelector('[data-admin-open-vehicle]').href=`vehicle.html?hf=${encodeURIComponent(currentVehicle.hotflash_id||currentVehicle.id)}`;previewWrap.innerHTML='<p class="small-muted">Click Preview FlashTag to render the printable artwork.</p>';result.hidden=false;setStatus('Vehicle found.','success');}catch(error){console.error('[Hot Flash admin lookup]',error);setStatus(error.message||'Vehicle could not be found.','error')}finally{busy=false;button.disabled=false;button.textContent='Find FlashTag'}});document.querySelector('[data-admin-preview]')?.addEventListener('click',async()=>{try{await renderCanvas()}catch(error){setRenderStatus(error.message||'FlashTag could not be rendered.','error')}});document.querySelector('[data-admin-download]')?.addEventListener('click',async()=>{try{const canvas=currentCanvas||await renderCanvas();downloadCanvas(canvas,flashTagFilename(currentVehicle,Boolean(founderNumber(currentVehicle))));setRenderStatus('PNG downloaded.','success')}catch(error){setRenderStatus(error.message||'FlashTag could not be downloaded.','error')}});document.querySelector('[data-admin-print]')?.addEventListener('click',async()=>{try{const canvas=currentCanvas||await renderCanvas();const image=canvas.toDataURL('image/png');const win=window.open('','_blank','noopener,noreferrer');if(!win)throw new Error('Allow pop-ups to print this FlashTag.');win.document.write(`<!doctype html><html><head><title>${currentVehicle.hotflash_id||'FlashTag'}</title><style>html,body{margin:0;background:#fff}body{display:grid;place-items:center;min-height:100vh}img{display:block;max-width:100%;height:auto}@media print{img{width:100%;max-width:none}}</style></head><body><img src="${image}" alt="Printable FlashTag"></body></html>`);win.document.close();win.focus();setTimeout(()=>win.print(),350);setRenderStatus('Print window opened.','success')}catch(error){setRenderStatus(error.message||'Print view could not be opened.','error')}});document.addEventListener('DOMContentLoaded',()=>checkAccess().catch(error=>{console.error('[Hot Flash admin access]',error);setStatus(error.message||'Admin access could not be verified.','error')}));