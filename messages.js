const listEl=document.querySelector('[data-conversation-list]'),threadEl=document.querySelector('[data-message-thread]'),threadWrap=document.querySelector('[data-thread]'),emptyEl=document.querySelector('[data-empty-thread]'),statusEl=document.querySelector('[data-message-status]'),newMessageForm=document.querySelector('[data-new-message-form]'),sendMessageForm=document.querySelector('[data-send-message-form]');let session,currentConversation=null,openingConversation=false,sendingMessage=false;const escapeHtml=value=>String(value??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const actions=()=>window.HotFlashActions;const setStatus=(message,type='')=>{if(!statusEl)return;statusEl.textContent=message||'';statusEl.className=`small-muted${type?` ${type}`:''}`};const friendlyError=(error,fallback)=>!navigator.onLine?'You appear to be offline. Check your connection and try again.':error?.code==='42501'?'Your session no longer has permission to do that. Please sign in again.':fallback||error?.message||'Something went wrong. Please try again.';async function stableSession(){return window.hotFlashGetStableSession?window.hotFlashGetStableSession():(await hotflashSupabase.auth.getSession()).data.session}async function init(){try{session=await stableSession();if(!session){location.href=`login.html?returnTo=${encodeURIComponent('messages.html'+location.search)}`;return}await loadConversations();const target=new URLSearchParams(location.search).get('u');if(target)await startConversation(target)}catch(error){setStatus(friendlyError(error,'Could not load messages.'),'error');if(listEl)listEl.innerHTML='<p class="error">Messages could not be loaded. Please refresh and try again.</p>'}}async function loadConversations(){listEl.innerHTML='<p class="small-muted">Loading conversations…</p>';const{data:memberships,error:membershipError}=await hotflashSupabase.from('conversation_members').select('conversation_id').eq('user_id',session.user.id);if(membershipError)throw membershipError;const ids=[...new Set((memberships||[]).map(row=>row.conversation_id))];if(!ids.length){listEl.innerHTML='<p class="small-muted">No conversations yet.</p>';return}const{data:members,error:memberError}=await hotflashSupabase.from('conversation_members').select('conversation_id,user_id,profiles(username,display_name)').in('conversation_id',ids).neq('user_id',session.user.id);if(memberError)throw memberError;listEl.innerHTML='';(members||[]).forEach(row=>{const button=document.createElement('button');button.type='button';button.className='conversation-card';button.dataset.conversationId=row.conversation_id;button.innerHTML=`<strong>${escapeHtml(row.profiles?.display_name||row.profiles?.username||'Member')}</strong><span class="small-muted">@${escapeHtml(row.profiles?.username||'member')}</span>`;button.addEventListener('click',()=>openConversation(row.conversation_id,button));listEl.appendChild(button)});if(!listEl.children.length)listEl.innerHTML='<p class="small-muted">No conversations yet.</p>'}async function startConversation(username){if(openingConversation)return;session=await stableSession();if(!session){location.href=`login.html?returnTo=${encodeURIComponent(location.pathname+location.search)}`;return}const button=newMessageForm?.querySelector('button[type="submit"]'),cleaned=String(username||'').trim().replace(/^@/,'');if(!/^[A-Za-z0-9_-]{3,32}$/.test(cleaned)){setStatus('Enter a valid Hot Flash username.','error');return}openingConversation=true;actions()?.begin(button,'Finding…');if(button)button.disabled=true;setStatus('Finding member…');try{const{data:person,error:profileError}=await hotflashSupabase.from('profiles').select('id,username').ilike('username',cleaned).maybeSingle();if(profileError)throw profileError;if(!person)throw new Error('Member not found.');if(person.id===session.user.id)throw new Error('You cannot message yourself.');const{data:conversationId,error:startError}=await hotflashSupabase.rpc('start_direct_conversation',{p_target_user_id:person.id});if(startError)throw startError;if(!conversationId)throw new Error('The conversation was not created.');setStatus('Conversation opened.','success');newMessageForm?.reset();actions()?.finish(button,'Opened ✓');await loadConversations();await openConversation(conversationId,listEl.querySelector(`[data-conversation-id="${CSS.escape(String(conversationId))}"]`))}catch(error){const message=friendlyError(error,'Could not start that conversation.');setStatus(message,'error');actions()?.fail(button);actions()?.error('Conversation was not started',message)}finally{openingConversation=false;if(button)button.disabled=false}}async function verifyMembership(id){const{data,error}=await hotflashSupabase.from('conversation_members').select('conversation_id').eq('conversation_id',id).eq('user_id',session.user.id).maybeSingle();if(error)throw error;if(!data)throw new Error('You no longer have access to this conversation.')}async function openConversation(id,button){if(!id||openingConversation)return;openingConversation=true;try{session=await stableSession();if(!session){location.href=`login.html?returnTo=${encodeURIComponent(location.pathname+location.search)}`;return}await verifyMembership(id);currentConversation=id;document.querySelectorAll('.conversation-card').forEach(item=>item.classList.remove('active'));button?.classList.add('active');emptyEl.hidden=true;threadWrap.hidden=false;threadEl.innerHTML='<p class="small-muted">Loading messages…</p>';await loadMessages();const{error:readError}=await hotflashSupabase.from('messages').update({read_at:new Date().toISOString()}).eq('conversation_id',id).neq('sender_id',session.user.id).is('read_at',null);if(readError)console.warn('[Hot Flash messages read status]',readError)}catch(error){threadEl.innerHTML=`<p class="error">${escapeHtml(friendlyError(error,'Could not load this conversation.'))}</p>`}finally{openingConversation=false}}async function loadMessages(highlightId){await verifyMembership(currentConversation);const{data,error}=await hotflashSupabase.from('messages').select('id,conversation_id,sender_id,body,created_at,read_at,profiles:sender_id(username,display_name)').eq('conversation_id',currentConversation).order('created_at').limit(500);if(error)throw error;threadEl.innerHTML=(data||[]).map(message=>`<div class="message-bubble ${message.sender_id===session.user.id?'mine':''}" data-message-id="${escapeHtml(message.id)}"><strong>${escapeHtml(message.profiles?.display_name||message.profiles?.username||'Member')}</strong><div>${escapeHtml(message.body)}</div><small>${escapeHtml(new Date(message.created_at).toLocaleString())}</small></div>`).join('')||'<p class="small-muted">No messages yet.</p>';const fresh=highlightId?threadEl.querySelector(`[data-message-id="${CSS.escape(String(highlightId))}"]`):null;actions()?.pulse(fresh);threadEl.scrollTop=threadEl.scrollHeight}newMessageForm?.addEventListener('submit',event=>{event.preventDefault();startConversation(new FormData(event.currentTarget).get('username'))});sendMessageForm?.addEventListener('submit',async event=>{event.preventDefault();if(!currentConversation||sendingMessage)return;session=await stableSession();if(!session){location.href=`login.html?returnTo=${encodeURIComponent(location.pathname+location.search)}`;return}const form=event.currentTarget,button=form.querySelector('button[type="submit"]'),body=String(new FormData(form).get('body')||'').trim();if(!body)return;if(body.length>4000){setStatus('Messages must be 4,000 characters or fewer.','error');return}sendingMessage=true;if(button)button.disabled=true;actions()?.begin(button,'Sending…');try{await verifyMembership(currentConversation);const{data,error}=await hotflashSupabase.from('messages').insert({conversation_id:currentConversation,sender_id:session.user.id,body}).select('id').single();if(error)throw error;form.reset();actions()?.finish(button,'Sent ✓');await loadMessages(data.id);setStatus('')}catch(error){const message=friendlyError(error,'Your message was not sent. Please try again.');actions()?.fail(button);actions()?.error('Message was not sent',message);setStatus(message,'error')}finally{sendingMessage=false;if(button)button.disabled=false}});document.addEventListener('DOMContentLoaded',init);